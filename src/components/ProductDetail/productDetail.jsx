import React from 'react';
import './productDetail.css';

const ProductDetail = () => {

    // axios requests below will go in app.jsx but just hashing them out for now

    // getAllProducts = async () => {
    //     let query = "https://localhost:44394/api/product"
    //     let allProducts = await axios.get(query);
    //     this.setState({});
    // }


    
    // getProductById = async (product) => {
    //     let query = `https://localhost:44394/api/product/${product.id}`;
    //     let productById = await axios.get(query);
    //     this.setSTate({});
    // }


    return(
        <div>
            This is a test
        </div>
    );
}

export default ProductDetail;